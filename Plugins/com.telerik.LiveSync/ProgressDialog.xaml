<UserControl x:Class="Telerik.BlackDragon.LiveSync.ProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Please wait while the latest changes are being retrieved..." TextWrapping="Wrap" />
		<ProgressBar x:Name="Progress" Grid.Row="1" Height="5" IsIndeterminate="False" Maximum="100" Margin="0,20,0,0" Padding="0"/>
	</Grid>
</UserControl>